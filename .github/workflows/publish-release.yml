name: GitHub Release Publisher

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

env:
  # Configuring Project
  PROJECT_PATH: src/AutoOS.App/AutoOS.App.csproj
  APP_NAME: AutoOS

  # Check Tag
  CHECK_TAG_EXISTENCE_BEFORE_CREATING_TAG: false

  # Custom Nuget Source
  IS_COMMUNITY_TOOLKIT_NUGET_SOURCE_ENABLED: true
  IS_CUSTOM_NUGET_SOURCE_ENABLED: false
  CUSTOM_NUGET_SOURCES: '' # Example ('https://api.nuget.org/v3/index.json, https://api.nuget.org/v2/index.json,...')

  # Configuring Dotnet Build Commands
  PUBLISH_OUTPUT_FOLDER: Publish
  PUBLISH_SELF_CONTAINED: true
  PUBLISH_SINGLE_FILE: false
  PUBLISH_READY_TO_RUN: true
  PUBLISH_AOT: false
  PUBLISH_TRIMMED: true
  PUBLISH_TRIM_MODE: partial # or full

  # Configuring GitHub Release
  IS_PRE_RELEASE: false
  SKIP_IF_RELEASE_EXIST: true
  MAKE_LATEST: true
  ALLOW_UPDATES: true
  ARTIFACT_ERRORS_FAIL_BUILD: false

jobs:
  build:
    runs-on: windows-2025-vs2026
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    outputs: # For accessing them from 'release' job
      app-version: ${{ steps.get-version.outputs.version }}
      IS_PRE_RELEASE: ${{ env.IS_PRE_RELEASE }}
    strategy:
      matrix:
        platform: [x64] # Change platform if you want to build only a specific platform

    steps:
    - uses: actions/checkout@v6
      with:
        fetch-depth: 0

    - uses: actions/setup-dotnet@v5
      with:
        dotnet-version: |
            10.x.x

    - name: Add msbuild to PATH
      uses: microsoft/setup-msbuild@v3
      with:
        vs-prerelease: true


    # Add CommunityToolkit Labs and Main nuget sources
    - run: |
        nuget sources add -name CommunityToolkit-Labs -source https://pkgs.dev.azure.com/dotnet/CommunityToolkit/_packaging/CommunityToolkit-Labs/nuget/v3/index.json
        nuget sources add -name CommunityToolkit-Main -source https://pkgs.dev.azure.com/dotnet/CommunityToolkit/_packaging/CommunityToolkit-MainLatest/nuget/v3/index.json
      if: contains(env.IS_COMMUNITY_TOOLKIT_NUGET_SOURCE_ENABLED, 'true')

    # Add Custom nuget sources
    - run: |
        $sources = $env:CUSTOM_NUGET_SOURCES -split ','
        $trimmedSources = $sources | ForEach-Object { $_.Trim() }
        $prefix = "CUSTOM_SOURCES_"
        for ($i = 0; $i -lt $trimmedSources.Length; $i++) {
          $sourceName = "${prefix}$($i + 1)"
          nuget sources add -name $sourceName -source $trimmedSources[$i]
        }
      if: contains(env.IS_CUSTOM_NUGET_SOURCE_ENABLED, 'true')

    # Reading version tag from the csproj file.
    - uses: kzrnm/get-net-sdk-project-versions-action@v3
      id: get-version
      with:
        proj-path: ${{ env.PROJECT_PATH }}

    # Build C++ project
    - name: Build SoundHelper
      run: msbuild src\AutoOS.App.Sound\AutoOS.App.Sound.vcxproj /p:Configuration=Release /p:Platform=${{ matrix.platform }} /p:PlatformToolset=v143

    # Restore the app
    - name: Restore the application
      run: dotnet restore $env:PROJECT_PATH /p:Configuration=Release /p:CppBuiltSeparately=true

    # Decode PFX
    - name: Decode the pfx
      run: |
        $pfx_cert_byte = [System.Convert]::FromBase64String("${{ secrets.BASE64_ENCODED_PFX }}")
        [IO.File]::WriteAllBytes("src/AutoOS.App/GitHubActionsWorkflow.pfx", $pfx_cert_byte)
      shell: pwsh

    - name: Inject secrets
      env:
        SECRETS_KEY: ${{ secrets.SECRETS_KEY }}
        DISCORD_WEBHOOK_BIOS: ${{ secrets.DISCORD_WEBHOOK_BIOS }}
        DISCORD_WEBHOOK_LOG: ${{ secrets.DISCORD_WEBHOOK_LOG }}
        DISCORD_WEBHOOK_ERROR: ${{ secrets.DISCORD_WEBHOOK_ERROR }}
        DISCORD_WEBHOOK_NETWORK: ${{ secrets.DISCORD_WEBHOOK_NETWORK }}
        SYNCFUSION_LICENSE_KEY: ${{ secrets.SYNCFUSION_LICENSE_KEY }}
      shell: pwsh
      run: |
        $keyBytes = [System.Convert]::FromBase64String($env:SECRETS_KEY)

        function Encrypt([string]$plain) {
            if ([string]::IsNullOrEmpty($plain)) { return "" }
            $bytes = [System.Text.Encoding]::UTF8.GetBytes($plain)
            for ($i = 0; $i -lt $bytes.Length; $i++) {
                $bytes[$i] = $bytes[$i] -bxor $keyBytes[$i % $keyBytes.Length]
            }
            return [System.Convert]::ToBase64String($bytes)
        }

        $bios = Encrypt $env:DISCORD_WEBHOOK_BIOS
        $log = Encrypt $env:DISCORD_WEBHOOK_LOG
        $err = Encrypt $env:DISCORD_WEBHOOK_ERROR
        $net = Encrypt $env:DISCORD_WEBHOOK_NETWORK
        $sync = Encrypt $env:SYNCFUSION_LICENSE_KEY
        $local = "src/AutoOS.Core/Secrets/Local.cs"
        $content = Get-Content $local -Raw
        $content = $content -replace 'Key = \[\]', "Key = System.Convert.FromBase64String(`"$($env:SECRETS_KEY)`")"
        $content = $content -replace 'GetEncryptedBios\(\) => \[\]', "GetEncryptedBios() => System.Convert.FromBase64String(`"$bios`")"
        $content = $content -replace 'GetEncryptedLog\(\) => \[\]', "GetEncryptedLog() => System.Convert.FromBase64String(`"$log`")"
        $content = $content -replace 'GetEncryptedError\(\) => \[\]', "GetEncryptedError() => System.Convert.FromBase64String(`"$err`")"
        $content = $content -replace 'GetEncryptedNetwork\(\) => \[\]', "GetEncryptedNetwork() => System.Convert.FromBase64String(`"$net`")"
        $content = $content -replace 'GetEncryptedSyncfusion\(\) => \[\]', "GetEncryptedSyncfusion() => System.Convert.FromBase64String(`"$sync`")"
        Set-Content -Path $local -Value $content -Encoding utf8NoBOM

    # Building with configured commands
    - name: Create the MSIX package
      run: dotnet publish $env:PROJECT_PATH -o ${{ env.PUBLISH_OUTPUT_FOLDER }}/${{ matrix.platform }} /p:Configuration=Release /p:Platform=${{ matrix.platform }} /p:RuntimeIdentifier=win-${{ matrix.platform }} /p:SelfContained=${{ env.PUBLISH_SELF_CONTAINED }} /p:PublishAot=${{ env.PUBLISH_AOT }} /p:PublishTrimmed=${{ env.PUBLISH_TRIMMED }} /p:TrimMode=${{ env.PUBLISH_TRIM_MODE }} /p:UapAppxPackageBuildMode=SideloadOnly /p:AppxBundle=Never /p:PackageCertificateKeyFile=GitHubActionsWorkflow.pfx /p:AppxPackageDir=../../Packages/ /p:GenerateAppxPackageOnBuild=true /p:CppBuiltSeparately=true
    
    # Remove PFX
    - name: Remove the pfx
      run: Remove-Item -path src/AutoOS.App/GitHubActionsWorkflow.pfx
      shell: pwsh

    # Zipping folder and all files
    - uses: vimtor/action-zip@v1.3
      with:
        files: ${{ env.PUBLISH_OUTPUT_FOLDER }}/${{ matrix.platform }}
        recursive: true
        dest: ${{ env.APP_NAME }}-v${{ steps.get-version.outputs.version }}-${{ matrix.platform }}.zip

    # Uploading all zip files to access them in the 'release' job
    - uses: actions/upload-artifact@v7
      with:
        name: artifacts-${{ matrix.platform }}
        path: Packages\

    # Checking version suffix for words like [alpha, beta, preview, and experiment]. Marking the release as a pre-release if any exists.
    - run: |
        # Define the list of strings
        $list = @("beta", "alpha", "preview", "experiment")

        # Define the suffix variable
        $suffix = "${{ steps.get-version.outputs.version-suffix }}"

        foreach ($item in $list) {
          # Convert both strings to lower case for case-insensitive comparison
          if ($suffix.ToLower().StartsWith($item.ToLower())) {
            echo "IS_PRE_RELEASE=true" >> $env:GITHUB_ENV
            break
          }
        }

  release:
    needs: build
    if: github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    env:
      # Read some variables from the 'build' job
      APP_VERSION: ${{ needs.build.outputs.app-version }}
      IS_PRE_RELEASE: ${{ needs.build.outputs.IS_PRE_RELEASE }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      # Downloading all zip files into the GitHub root directory (uploaded in 'build' job)
      - uses: actions/download-artifact@v8
        with:
          merge-multiple: true

      # Configuring git to create a tag
      - run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"

          TAG_NAME="v${{ env.APP_VERSION }}"
          if [[ "${{ env.CHECK_TAG_EXISTENCE_BEFORE_CREATING_TAG }}" == "true" ]]; then
              git fetch --tags
              if [[ $(git tag -l "$TAG_NAME") ]]; then
                  echo "Tag found (already exist). Skipping to the next step"
              else
                  echo "Tag not found, creating new tag"
                  git tag "$TAG_NAME"
                  git push origin "$TAG_NAME"
              fi
          else
              echo "Creating new tag"
              git tag "$TAG_NAME"
              git push origin "$TAG_NAME"
          fi  
          
      # Installing a package for generating a changelog file
      - run: npm install -g auto-changelog  
      - run: auto-changelog --tag-pattern .+ --commit-limit false --starting-version v${{ env.APP_VERSION }} --ending-version v${{ env.APP_VERSION }} --hide-credit  
      
      - name: Rename output files
        run: |
          folder="${APP_NAME}.App_${APP_VERSION}_x64_Test"
          mv "$folder"/*.msix "$folder/AutoOS.msix"
          mv "$folder"/*.cer "$folder/AutoOS.cer"

      # Creating a Release in GitHub
      - uses: ncipollo/release-action@v1
        with:
          artifacts: |
              ${{ env.APP_NAME }}.App_${{ env.APP_VERSION }}_x64_Test/AutoOS.msix
              ${{ env.APP_NAME }}.App_${{ env.APP_VERSION }}_x64_Test/AutoOS.cer
          bodyFile: "CHANGELOG.md"
          name: v${{ env.APP_VERSION }}
          tag: v${{ env.APP_VERSION }}
          prerelease: ${{ env.IS_PRE_RELEASE }}
          skipIfReleaseExists: ${{ env.SKIP_IF_RELEASE_EXIST }}
          makeLatest: ${{ env.MAKE_LATEST }}
          allowUpdates: ${{ env.ALLOW_UPDATES }}
          artifactErrorsFailBuild: ${{ env.ARTIFACT_ERRORS_FAIL_BUILD }}
