<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="AutoOS.Views.Settings.SchedulingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:dev="using:DevWinUI"
      xmlns:local="using:AutoOS"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:views="using:AutoOS.Views"
      mc:Ignorable="d">
    <ScrollView Margin="{ThemeResource ContentPageMargin}"
                HorizontalAlignment="Stretch"
                VerticalScrollBarVisibility="Hidden">
        <StackPanel Margin="10"
                    dev:PanelAttach.ChildrenTransitions="Default"
                    Spacing="5">
            <TextBlock Text="Per-CPU Scheduling"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Margin="7,-11,0,57"/>
            <dev:SettingsGroup Description="Benchmark your GPU and XHCI Controller Affinities or set them manually. If your CPU has 6 or more cores they will be reserved."
                               Header="Per-CPU Scheduling"
                               HeaderIcon="{dev:BitmapIcon Source=Assets/Fluent/Cpu.png}">
                <dev:SettingsGroup.Content>
                    <Button Content="Benchmark"
                            
                            Style="{StaticResource AccentButtonStyle}"
                            HorizontalAlignment="Left"/>
                </dev:SettingsGroup.Content>
                <dev:SettingsGroup.Items>
                    <dev:SettingsCard Header="GPU Affinity">
                        <ComboBox x:Name="GPU"
                                  SelectionChanged="Gpu_Changed"/>
                    </dev:SettingsCard>
                    <dev:SettingsCard Header="XHCI Controller Affinity">
                        <ComboBox x:Name="XHCI"
                                  SelectionChanged="Xhci_Changed"/>
                    </dev:SettingsCard>
                </dev:SettingsGroup.Items>
            </dev:SettingsGroup>
            <StackPanel x:Name="AffinityInfo"/>
        </StackPanel>
    </ScrollView>
</Page>
