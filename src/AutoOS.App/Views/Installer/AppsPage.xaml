<?xml version="1.0" encoding="utf-8" ?>
<Page
	x:Class="AutoOS.Views.Installer.AppsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:CommunityToolkit.WinUI.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:dev="using:DevWinUI"
	xmlns:local="using:AutoOS"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:views="using:AutoOS.Views.Installer"
	mc:Ignorable="d">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<TextBlock
			Margin="36,24,0,0"
			Style="{StaticResource TitleTextBlockStyle}"
			Text="Applications" />
		<ScrollViewer
			Grid.Row="1"
			VerticalScrollBarVisibility="Auto"
			VerticalScrollMode="Auto">
			<StackPanel
				Margin="{ThemeResource ContentPageMargin}"
				Padding="{ThemeResource ContentPagePadding}"
				dev:PanelAttach.ChildrenTransitions="Default">
				<dev:Card
					Title="Messaging"
					Margin="0,0,0,8"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="Messaging"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="Messaging_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
				<dev:Card
					Title="Launchers"
					Margin="0,0,0,8"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="Launchers"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="Launchers_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
				<dev:Card
					Title="Music"
					Margin="0,0,0,8"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="Music"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="Music_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
				<dev:Card
					Title="Peripherals"
					Margin="0,0,0,8"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="Peripherals"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="Peripherals_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
				<dev:Card
					Title="Controllers"
					Margin="0,0,0,8"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="Controllers"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="Controllers_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
				<dev:Card
					Title="Development"
					Margin="0,0,0,8"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="Development"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="Development_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
				<dev:Card
					Title="Sysinternals"
					Margin="0,0,0,8"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="Sysinternals"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="Sysinternals_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
				<dev:Card
					Title="Overclocking"
					Margin="0,0,0,8"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="Overclocking"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="Overclocking_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
				<dev:Card
					Title="Music Production"
					Margin="0,0,0,8"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="MusicProduction"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="MusicProduction_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
				<dev:Card
					Title="Video Production"
					Margin="0,0,0,8"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="VideoProduction"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="VideoProduction_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
				<dev:Card
					Title="Multimedia"
					Margin="0,0,0,8"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="Multimedia"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="Multimedia_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
				<dev:Card
					Title="Office"
					Margin="0,0,0,8"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="Office"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="Office_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
				<dev:Card
					Title="Miscellaneous"
					Margin="0,0,0,36"
					Padding="24,24,24,12">
					<dev:Card.Content>
						<GridView
							x:Name="Miscellaneous"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="Miscellaneous_Changed"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
			</StackPanel>
		</ScrollViewer>
	</Grid>
</Page>
