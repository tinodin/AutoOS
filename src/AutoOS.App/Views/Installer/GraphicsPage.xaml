<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="AutoOS.Views.Installer.GraphicsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:controls="using:CommunityToolkit.WinUI.Controls"
      xmlns:converters="using:CommunityToolkit.WinUI.Converters"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:dev="using:DevWinUI"
      xmlns:local="using:AutoOS"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:gpuModels="using:AutoOS.Core.Helpers.GPU.Models"
      xmlns:gpuConverters="using:AutoOS.Core.Helpers.GPU.Converters"
      xmlns:views="using:AutoOS.Views"
      mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Margin="36,24,0,0"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Text="Graphics Cards" />
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Auto">
            <StackPanel Margin="{ThemeResource ContentPageMargin}"
                        Padding="{ThemeResource ContentPagePadding}"
                        dev:PanelAttach.ChildrenTransitions="Default">
                <ItemsRepeater x:Name="GpuRepeater"
                               ItemsSource="{x:Bind GPUs, Mode=OneWay}">
                    <ItemsRepeater.ItemTemplate>
                        <DataTemplate x:DataType="gpuModels:GpuInfo">
                            <dev:SettingsGroup Margin="0,0,0,8"
                                               Description="{x:Bind CurrentVersion, Converter={StaticResource CurrentVersionConverter}}"
                                               Header="{x:Bind DeviceName}"
                                               HeaderIcon="{x:Bind VendorId, Converter={StaticResource VendorIdToImageIconConverter}}">
                                <dev:SettingsGroup.Content>
                                    <ToggleSwitch IsOn="{x:Bind Install, Mode=TwoWay}"
                                                  OffContent="Don't install"
                                                  OnContent="Install" />
                                </dev:SettingsGroup.Content>
                                <dev:SettingsGroup.Items>
                                    <dev:SettingsCard Description="Disabling Dynamic Performance States (P-States) locks the GPU at its maximum clock frequency which increases temperatures and power consumption."
                                                      Header="Dynamic Performance States (P-States)"
                                                      Visibility="{x:Bind NVIDIA, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <ToggleSwitch DataContext="{x:Bind}"
                                                      IsOn="{x:Bind PStates, Mode=TwoWay}" />
                                    </dev:SettingsCard>
                                    <dev:SettingsCard Description="Disabling Error Correction Code (ECC) can slightly increase memory performance and bandwidth but removes protection against silent data corruption and memory-related system crashes."
                                                      Header="Error Code Correction (ECC)"
                                                      Visibility="{x:Bind ECCSupport, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <ToggleSwitch DataContext="{x:Bind}"
                                                      IsOn="{x:Bind ECC, Mode=TwoWay}" />
                                    </dev:SettingsCard>
                                    <dev:SettingsCard Description="Enabling GPU System Processor (GSP) Firmware offloads driver tasks from the CPU to the GPU System Processor (GSP) for lower latency but can cause driver / display issues that requires you to go into safe mode to disable it."
                                                      Header="GPU System Processor (GSP) Firmware"
                                                      Visibility="{x:Bind RTX, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <ToggleSwitch DataContext="{x:Bind}"
                                                      IsOn="{x:Bind GspFirmware, Mode=TwoWay}" />
                                    </dev:SettingsCard>
                                    <dev:SettingsCard Description="Disabling High-Bandwidth Digital Content Protection (HDCP) breaks Netflix and other protected video streaming services."
                                                      Header="High-Bandwidth Digital Content Protection (HDCP)"
                                                      Visibility="{x:Bind NVIDIA, Converter={StaticResource BoolToVisibilityConverter}}">
                                        <ToggleSwitch DataContext="{x:Bind}"
                                                      IsOn="{x:Bind HDCP, Mode=TwoWay}" />
                                    </dev:SettingsCard>
                                    <dev:SettingsCard Description="Disabling High-Definition Multimedia Interface (HDMI)/DisplayPort (DP) Audio prevents audio devices connected to the graphics card from showing up."
                                                      Header="High-Definition Multimedia Interface (HDMI)/DisplayPort (DP) Audio">
                                        <ToggleSwitch DataContext="{x:Bind}"
                                                      IsOn="{x:Bind HDMIDPAudio, Mode=TwoWay}" />
                                    </dev:SettingsCard>
                                </dev:SettingsGroup.Items>
                            </dev:SettingsGroup>
                        </DataTemplate>
                    </ItemsRepeater.ItemTemplate>
                </ItemsRepeater>
                <dev:SettingsGroup Description="Import your MSI Afterburner profile here."
                                   Header="MSI Afterburner">
                    <dev:SettingsGroup.HeaderIcon>
                        <ImageIcon Source="/Assets/Fluent/MsiAfterburner.png" />
                    </dev:SettingsGroup.HeaderIcon>
                    <dev:SettingsGroup.Content>
                        <Button HorizontalAlignment="Left"
                                Click="BrowseMsi_Click"
                                Content="Import profile"
                                Style="{ThemeResource AccentButtonStyle}" />
                    </dev:SettingsGroup.Content>
                </dev:SettingsGroup>
                <StackPanel x:Name="MsiAfterburnerInfo"
                            Margin="4,8,4,0" />
                <dev:SettingsGroup Margin="0,0,0,36"
                                   Description="Launch OBS Studio on startup and start the replay buffer."
                                   Header="OBS Studio">
                    <dev:SettingsGroup.HeaderIcon>
                        <ImageIcon Source="/Assets/Fluent/Obs.png" />
                    </dev:SettingsGroup.HeaderIcon>
                    <dev:SettingsGroup.Content>
                        <ToggleSwitch x:Name="OBS"
                                      Toggled="OBS_Toggled">
                        </ToggleSwitch>
                    </dev:SettingsGroup.Content>
                </dev:SettingsGroup>
            </StackPanel>
        </ScrollViewer>
    </Grid>
    <Page.Resources>
        <gpuConverters:VendorIdToImageIconConverter x:Key="VendorIdToImageIconConverter" />
        <gpuConverters:CurrentVersionConverter x:Key="CurrentVersionConverter" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>
</Page>