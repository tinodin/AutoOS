<?xml version="1.0" encoding="utf-8" ?>
<Window
	x:Class="AutoOS.App.Views.MainWindow"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:dev="using:DevWinUI"
	xmlns:local="using:AutoOS"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">
	<Grid x:Name="RootGrid" Loaded="RootGrid_Loaded">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition />
		</Grid.RowDefinitions>

		<!--  Background Image  -->
		<Image
			Grid.RowSpan="2"
			HorizontalAlignment="{x:Bind ViewModel.BackgroundImageHorizontalAlignment, Mode=OneWay}"
			VerticalAlignment="{x:Bind ViewModel.BackgroundImageVerticalAlignment, Mode=OneWay}"
			Opacity="{x:Bind ViewModel.BackgroundImageOpacity, Mode=OneWay}"
			Source="{x:Bind ViewModel.BackgroundImageSource, Mode=OneWay}"
			Stretch="{x:Bind ViewModel.BackgroundImageFit, Mode=OneWay}" />

		<TitleBar
			x:Name="AppTitleBar"
			IsBackButtonVisible="False"
			IsPaneToggleButtonVisible="True"
			Subtitle="{x:Bind dev:ProcessInfoHelper.VersionWithPrefix}">
			<TitleBar.LeftHeader>
				<ImageIcon
					Width="16"
					Height="16"
					Margin="4,0,8,0"
					PointerPressed="AppIcon_PointerPressed"
					Source="/Assets/AppIcon.png" />
			</TitleBar.LeftHeader>
		</TitleBar>
		<NavigationView
			x:Name="NavView"
			Grid.Row="1"
			IsBackButtonVisible="Collapsed"
			IsPaneToggleButtonVisible="False"
			IsSettingsVisible="False"
			OpenPaneLength="300">
			<NavigationView.Header>
				<dev:BreadcrumbNavigator x:Name="BreadCrumbNav" />
			</NavigationView.Header>
			<Frame x:Name="NavFrame" />
		</NavigationView>
	</Grid>
</Window>
