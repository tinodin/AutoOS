<?xml version="1.0" encoding="utf-8" ?>
<Page
	x:Class="AutoOS.App.Views.Settings.BrowsersPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:CommunityToolkit.WinUI.Controls"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:dev="using:DevWinUI"
	xmlns:local="using:AutoOS"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:views="using:AutoOS.App.Views.Settings"
	mc:Ignorable="d">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<Grid Margin="36,24,36,0">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="*" />
				<ColumnDefinition Width="Auto" />
			</Grid.ColumnDefinitions>
			<TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Browsers" />
			<Button
				x:Name="InstallButton"
				Grid.Column="1"
				Click="InstallButton_Click"
				Content="Install selected"
				IsEnabled="False"
				Style="{StaticResource AccentButtonStyle}" />
		</Grid>
		<ScrollViewer
			Grid.Row="1"
			VerticalScrollBarVisibility="Auto"
			VerticalScrollMode="Auto">
			<StackPanel
				Margin="{ThemeResource ContentPageMargin}"
				Padding="{ThemeResource ContentPagePadding}"
				dev:PanelAttach.ChildrenTransitions="Default">
				<dev:Card
					Title="Browsers"
					Margin="0,0,0,8"
					Padding="24,24,24,12"
					Visibility="{x:Bind GetVisibility(browserItems.Count), Mode=OneWay}">
					<dev:Card.Content>
						<GridView
							x:Name="Browsers"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="GridView_SelectionChanged"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>

				<dev:Card
					Title="Extensions"
					Margin="0,0,0,36"
					Padding="24,24,24,12"
					Visibility="{x:Bind GetVisibility(browserItems.Count), Mode=OneWay}">
					<dev:Card.Content>
						<GridView
							x:Name="Extensions"
							IsMultiSelectCheckBoxEnabled="False"
							SelectionChanged="GridView_SelectionChanged"
							SelectionMode="Multiple">
							<GridView.ItemTemplate>
								<DataTemplate x:DataType="views:GridViewItem">
									<Grid Height="85" Margin="4">
										<Grid.RowDefinitions>
											<RowDefinition Height="*" />
										</Grid.RowDefinitions>
										<StackPanel VerticalAlignment="Center">
											<Image Height="36" Margin="0,0,0,4">
												<Image.Source>
													<BitmapImage
														DecodePixelHeight="36"
														DecodePixelType="Logical"
														UriSource="{x:Bind ImageSource, Mode=OneTime}" />
												</Image.Source>
											</Image>
											<TextBlock
												HorizontalAlignment="Center"
												FontSize="14"
												Text="{x:Bind Text, Mode=OneTime}"
												TextAlignment="Center"
												TextTrimming="CharacterEllipsis"
												TextWrapping="Wrap" />
										</StackPanel>
									</Grid>
								</DataTemplate>
							</GridView.ItemTemplate>
							<GridView.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:StaggeredPanel
										ColumnSpacing="4"
										DesiredColumnWidth="145"
										RowSpacing="12" />
								</ItemsPanelTemplate>
							</GridView.ItemsPanel>
						</GridView>
					</dev:Card.Content>
				</dev:Card>
			</StackPanel>
		</ScrollViewer>
	</Grid>
</Page>
