<Page
	x:Class="AutoOS.App.Views.Settings.Scheduling.SchedulingDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="using:CommunityToolkit.WinUI.Controls"
	xmlns:dev="using:DevWinUI"
	xmlns:models="using:AutoOS.Core.Data.Models.CPU"
	xmlns:vm="using:AutoOS.App.Views.Settings.Scheduling.ViewModels">
	<Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<TextBlock
			Margin="0,-8,0,24"
			Foreground="{ThemeResource TextFillColorSecondaryBrush}"
			Style="{StaticResource BodyTextBlockStyle}"
			Text="{x:Bind Location, Mode=OneTime}" />
		<ScrollViewer
			Grid.Row="1"
			HorizontalScrollBarVisibility="Disabled"
			HorizontalScrollMode="Disabled"
			VerticalScrollBarVisibility="Hidden"
			VerticalScrollMode="Auto">
			<StackPanel Spacing="12">
				<Grid Grid.Row="0" ColumnSpacing="12">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*" />
						<ColumnDefinition Width="*" />
					</Grid.ColumnDefinitions>
					<dev:Card
						Title="MSI Policies"
						Grid.Column="0"
						HorizontalAlignment="Stretch">
						<StackPanel Spacing="12">
							<CheckBox Content="MSI Mode:" IsChecked="{x:Bind ViewModel.MsiSupported, Mode=TwoWay}" />
							<Grid ColumnSpacing="8">
								<Grid.ColumnDefinitions>
									<ColumnDefinition Width="Auto" />
									<ColumnDefinition Width="*" />
								</Grid.ColumnDefinitions>
								<TextBlock
									Grid.Column="0"
									VerticalAlignment="Center"
									Text="MSI Limit:" />
								<NumberBox
									Grid.Column="1"
									IsEnabled="{x:Bind ViewModel.IsMsiLimitEnabled, Mode=OneWay}"
									Maximum="{x:Bind ViewModel.EffectiveMaxMsiLimit, Mode=OneWay}"
									Minimum="0"
									SpinButtonPlacementMode="Inline"
									Value="{x:Bind ViewModel.MsiLimit, Mode=TwoWay}" />
							</Grid>
						</StackPanel>
					</dev:Card>
					<dev:Card
						Title="Advanced Policies"
						Grid.Column="1"
						HorizontalAlignment="Stretch">
						<Grid ColumnSpacing="12" RowSpacing="12">
							<Grid.ColumnDefinitions>
								<ColumnDefinition Width="Auto" />
								<ColumnDefinition Width="*" />
							</Grid.ColumnDefinitions>
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto" />
								<RowDefinition Height="Auto" />
							</Grid.RowDefinitions>
							<TextBlock
								Grid.Row="0"
								Grid.Column="0"
								VerticalAlignment="Center"
								Text="Device Priority:" />
							<ComboBox
								Grid.Row="0"
								Grid.Column="1"
								HorizontalAlignment="Stretch"
								ItemsSource="{x:Bind ViewModel.IrqPriorities, Mode=OneWay}"
								SelectedIndex="{x:Bind ViewModel.DevicePriority, Mode=TwoWay}">
								<ComboBox.ItemTemplate>
									<DataTemplate x:DataType="vm:IrqPriorityItem">
										<TextBlock Text="{x:Bind Name}" />
									</DataTemplate>
								</ComboBox.ItemTemplate>
							</ComboBox>
							<TextBlock
								Grid.Row="1"
								Grid.Column="0"
								VerticalAlignment="Center"
								Text="Device Policy:" />
							<ComboBox
								Grid.Row="1"
								Grid.Column="1"
								HorizontalAlignment="Stretch"
								ItemsSource="{x:Bind ViewModel.IrqPolicies, Mode=OneWay}"
								SelectedIndex="{x:Bind ViewModel.DevicePolicy, Mode=TwoWay}">
								<ComboBox.ItemTemplate>
									<DataTemplate x:DataType="vm:IrqPolicyItem">
										<TextBlock Text="{x:Bind Name}" />
									</DataTemplate>
								</ComboBox.ItemTemplate>
							</ComboBox>
						</Grid>
					</dev:Card>
				</Grid>
				<Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="{x:Bind ViewModel.Group0Width, Mode=OneWay}" />
						<ColumnDefinition Width="{x:Bind ViewModel.Group1Width, Mode=OneWay}" />
						<ColumnDefinition Width="{x:Bind ViewModel.Group2Width, Mode=OneWay}" />
					</Grid.ColumnDefinitions>
					<dev:Card
						Title="{x:Bind ViewModel.Group0.Name, Mode=OneWay}"
						Grid.Column="0"
						Margin="{x:Bind ViewModel.Group0Margin, Mode=OneWay}"
						HorizontalAlignment="Stretch"
						VerticalAlignment="Stretch">
						<ItemsControl
							x:Name="Group0ItemsControl"
							HorizontalAlignment="Stretch"
							VerticalAlignment="Top"
							DataContext="{x:Bind ViewModel.Group0, Mode=OneWay}"
							ItemsSource="{x:Bind ViewModel.Group0.Cores, Mode=OneWay}"
							Loaded="GroupItemsControl_Loaded">
							<ItemsControl.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:UniformGrid HorizontalAlignment="Stretch" VerticalAlignment="Top" />
								</ItemsPanelTemplate>
							</ItemsControl.ItemsPanel>
							<ItemsControl.ItemTemplate>
								<DataTemplate x:DataType="models:CpuCore">
									<dev:Card
										Title="{x:Bind Name}"
										Margin="4"
										HorizontalAlignment="Stretch"
										VerticalAlignment="Top">
										<Grid>
											<ItemsRepeater HorizontalAlignment="Stretch" ItemsSource="{x:Bind Threads}">
												<ItemsRepeater.ItemTemplate>
													<DataTemplate x:DataType="models:CpuThread">
														<CheckBox Content="{x:Bind Name}" IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />
													</DataTemplate>
												</ItemsRepeater.ItemTemplate>
											</ItemsRepeater>
										</Grid>
									</dev:Card>
								</DataTemplate>
							</ItemsControl.ItemTemplate>
						</ItemsControl>
					</dev:Card>
					<dev:Card
						Title="{x:Bind ViewModel.Group1.Name, Mode=OneWay}"
						Grid.Column="1"
						Margin="{x:Bind ViewModel.Group1Margin, Mode=OneWay}"
						HorizontalAlignment="Stretch"
						VerticalAlignment="Stretch"
						Visibility="{x:Bind ViewModel.Group1Visibility, Mode=OneWay}">
						<ItemsControl
							x:Name="Group1ItemsControl"
							HorizontalAlignment="Stretch"
							VerticalAlignment="Top"
							DataContext="{x:Bind ViewModel.Group1, Mode=OneWay}"
							ItemsSource="{x:Bind ViewModel.Group1.Cores, Mode=OneWay}"
							Loaded="GroupItemsControl_Loaded">
							<ItemsControl.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:UniformGrid HorizontalAlignment="Stretch" VerticalAlignment="Top" />
								</ItemsPanelTemplate>
							</ItemsControl.ItemsPanel>
							<ItemsControl.ItemTemplate>
								<DataTemplate x:DataType="models:CpuCore">
									<dev:Card
										Title="{x:Bind Name}"
										Margin="4"
										HorizontalAlignment="Stretch"
										VerticalAlignment="Top">
										<Grid>
											<ItemsRepeater HorizontalAlignment="Stretch" ItemsSource="{x:Bind Threads}">
												<ItemsRepeater.ItemTemplate>
													<DataTemplate x:DataType="models:CpuThread">
														<CheckBox Content="{x:Bind Name}" IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />
													</DataTemplate>
												</ItemsRepeater.ItemTemplate>
											</ItemsRepeater>
										</Grid>
									</dev:Card>
								</DataTemplate>
							</ItemsControl.ItemTemplate>
						</ItemsControl>
					</dev:Card>
					<dev:Card
						Title="{x:Bind ViewModel.Group2.Name, Mode=OneWay}"
						Grid.Column="2"
						Margin="{x:Bind ViewModel.Group2Margin, Mode=OneWay}"
						HorizontalAlignment="Stretch"
						VerticalAlignment="Stretch"
						Visibility="{x:Bind ViewModel.Group2Visibility, Mode=OneWay}">
						<ItemsControl
							x:Name="Group2ItemsControl"
							HorizontalAlignment="Stretch"
							VerticalAlignment="Top"
							DataContext="{x:Bind ViewModel.Group2, Mode=OneWay}"
							ItemsSource="{x:Bind ViewModel.Group2.Cores, Mode=OneWay}"
							Loaded="GroupItemsControl_Loaded">
							<ItemsControl.ItemsPanel>
								<ItemsPanelTemplate>
									<controls:UniformGrid HorizontalAlignment="Stretch" VerticalAlignment="Top" />
								</ItemsPanelTemplate>
							</ItemsControl.ItemsPanel>
							<ItemsControl.ItemTemplate>
								<DataTemplate x:DataType="models:CpuCore">
									<dev:Card
										Title="{x:Bind Name}"
										Margin="4"
										HorizontalAlignment="Stretch"
										VerticalAlignment="Top">
										<Grid>
											<ItemsRepeater HorizontalAlignment="Stretch" ItemsSource="{x:Bind Threads}">
												<ItemsRepeater.ItemTemplate>
													<DataTemplate x:DataType="models:CpuThread">
														<CheckBox Content="{x:Bind Name}" IsChecked="{x:Bind IsSelected, Mode=TwoWay}" />
													</DataTemplate>
												</ItemsRepeater.ItemTemplate>
											</ItemsRepeater>
										</Grid>
									</dev:Card>
								</DataTemplate>
							</ItemsControl.ItemTemplate>
						</ItemsControl>
					</dev:Card>
				</Grid>
			</StackPanel>
		</ScrollViewer>
	</Grid>
</Page>
