<?xml version="1.0" encoding="utf-8" ?>
<Page x:Class="AutoOS.Views.Settings.ServicesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:dev="using:DevWinUI"
      xmlns:local="using:AutoOS"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:views="using:AutoOS.Views"
      mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Margin="36,24,0,0"
                   Style="{StaticResource TitleTextBlockStyle}"
                   Text="Services &amp; Drivers" />
        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      VerticalScrollMode="Auto">
            <StackPanel Margin="{ThemeResource ContentPageMargin}"
                        Padding="{ThemeResource ContentPagePadding}"
                        dev:PanelAttach.ChildrenTransitions="Default">
                <dev:SettingsGroup x:Name="Services_SettingsGroup"
                                   Margin="0,0,0,36"
                                   Description="Toggle functionality needed in service disabled state and toggle between service states. &#x0A;Disable the toggle whenever you are gaming competitively. Enable it again if you need functionality back for work or installing applications / drivers."
                                   Header="Service-list-builder">
                    <dev:SettingsGroup.HeaderIcon>
                        <ImageIcon Source="/Assets/Fluent/Services.png" />
                    </dev:SettingsGroup.HeaderIcon>
                    <dev:SettingsGroup.Content>
                        <ToggleSwitch x:Name="Services"
                                      Toggled="Services_Toggled" />
                    </dev:SettingsGroup.Content>
                    <dev:SettingsGroup.Items>
                        <dev:SettingsCard ContentAlignment="Left">
                            <CheckBox x:Name="WIFI"
                                      Margin="0,0,0,15"
                                      Padding="22,5,0,0"
                                      Click="WIFI_Checked"
                                      Content="WiFi support" />
                        </dev:SettingsCard>
                        <dev:SettingsCard ContentAlignment="Left">
                            <CheckBox x:Name="Bluetooth"
                                      Margin="0,0,0,15"
                                      Padding="22,5,0,0"
                                      Click="Bluetooth_Checked"
                                      Content="Bluetooth support" />
                        </dev:SettingsCard>
                    </dev:SettingsGroup.Items>
                </dev:SettingsGroup>
                <StackPanel x:Name="ServiceInfo" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
