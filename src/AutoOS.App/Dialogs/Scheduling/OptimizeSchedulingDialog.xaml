<ContentDialog
	x:Class="AutoOS.App.Dialogs.Scheduling.OptimizeSchedulingDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:scheduling="using:AutoOS.App.Views.Settings.Scheduling"
	Title="{x:Bind ViewModel.Title, Mode=OneWay}"
	CloseButtonText="{x:Bind ViewModel.CloseButtonText, Mode=OneWay}"
	DefaultButton="Close"
	IsPrimaryButtonEnabled="{x:Bind ViewModel.IsPrimaryButtonEnabled, Mode=OneWay}"
	PrimaryButtonText="{x:Bind ViewModel.PrimaryButtonText, Mode=OneWay}"
	mc:Ignorable="d">

	<ContentDialog.Resources>
		<Style BasedOn="{StaticResource DefaultContentDialogStyle}" TargetType="ContentDialog" />
	</ContentDialog.Resources>

	<ListView
		x:Name="Devices"
		ItemsSource="{x:Bind ViewModel.Devices}"
		Loaded="Devices_Loaded"
		SelectionChanged="Devices_SelectionChanged"
		SelectionMode="Multiple">
		<ListView.ItemTemplate>
			<DataTemplate x:DataType="scheduling:SchedulingItem">
				<StackPanel Margin="12,6,0,6">
					<TextBlock
						FontWeight="Normal"
						Text="{x:Bind Name, Mode=OneWay}" />

					<TextBlock
						Foreground="{ThemeResource TextFillColorSecondaryBrush}"
						Style="{StaticResource CaptionTextBlockStyle}"><Run Text="{x:Bind DeviceType, Mode=OneWay}" /><Run Text=" • " /><Run Text="{x:Bind Location, Mode=OneWay}" /></TextBlock>
				</StackPanel>
			</DataTemplate>
		</ListView.ItemTemplate>
	</ListView>
</ContentDialog>
