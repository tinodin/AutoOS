<ContentDialog
	x:Class="AutoOS.App.Dialogs.Bios.BiosPasswordDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="using:AutoOS.App.Dialogs.Bios"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:wctconverters="using:CommunityToolkit.WinUI.Converters"
	Title="{x:Bind ViewModel.Title, Mode=OneWay}"
	CloseButtonText="{x:Bind ViewModel.CloseButtonText, Mode=OneWay}"
	DefaultButton="Primary"
	IsPrimaryButtonEnabled="{x:Bind ViewModel.IsPrimaryButtonEnabled, Mode=OneWay}"
	PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
	PrimaryButtonText="{x:Bind ViewModel.PrimaryButtonText, Mode=OneWay}"
	mc:Ignorable="d">

	<ContentDialog.Resources>
		<Style BasedOn="{StaticResource DefaultContentDialogStyle}" TargetType="local:BiosPasswordDialog" />
		<wctconverters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
	</ContentDialog.Resources>

	<StackPanel Spacing="4">

		<!--  Password Box  -->
		<PasswordBox
			x:Name="PasswordBox"
			Header="BIOS administrator password:"
			PasswordChanged="PasswordBox_PasswordChanged"
			PasswordRevealMode="Peek"
			PlaceholderText="Enter password" />

		<!--  Error  -->
		<TextBlock
			Foreground="{ThemeResource SystemFillColorCriticalBrush}"
			Style="{StaticResource CaptionTextBlockStyle}"
			Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}"
			TextWrapping="Wrap"
			Visibility="{x:Bind ViewModel.HasError, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}}" />
	</StackPanel>
</ContentDialog>
